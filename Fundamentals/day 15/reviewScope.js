//Scope ---> {...} --->local scope --
// variabel yang dideclare di outer seconds scope, dapat diakses di local scope
// variabel yang dideclare di local scope, tidak dapat diakses di outer seconds scope

